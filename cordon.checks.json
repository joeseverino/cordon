{
  "$schema": "./checks/config.schema.json",
  "idempotence": {
    "command": "node checks/run.mjs --schema > checks/config.schema.json"
  },
  "commands": [
    {
      "id": "conformance",
      "name": "Conformance + engine self-test",
      "effect": "read",
      "requires": ["node"],
      "exec": { "cmd": "npm", "args": ["test"] },
      "fix": "Run `npm test` (conformance/validate.mjs + checks/selftest.mjs) and fix the reported divergence; if a contract shape changed legitimately, update the schema, fixtures, and emitters together."
    },
    {
      "id": "py-emitter",
      "name": "Python reference emitter selftest",
      "effect": "read",
      "requires": ["python3"],
      "exec": { "cmd": "python3", "args": ["emitters/python/selftest.py"] },
      "fix": "Run `python3 emitters/python/selftest.py` and fix the reported divergence; if the contract shape changed legitimately, update the emitter and fixtures together."
    }
  ]
}
